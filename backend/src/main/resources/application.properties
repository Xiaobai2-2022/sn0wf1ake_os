spring.application.name=backend

# MySQL Database Configuration for Testing
spring.datasource.url=jdbc:mysql://localhost:3306/myappdb_test?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Sn0wf1ake_dev
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Properties
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Logging Level
logging.level.root=WARN

# Add the JDBC URL
spring.datasource.url=jdbc:mysql://localhost:3306/sfproddb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# My SQL Configuration

